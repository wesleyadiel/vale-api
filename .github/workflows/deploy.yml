name: Deploy Prod

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: 🚀 Deploy App
    runs-on: ubuntu-latest

    steps:
      - name: ⏬ Checkout código
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      - name: 🛠️ Build com Maven
        run: mvn clean package -DskipTests

      - name: 🔐 Preparar chave SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.LIGHTSAIL_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: 🛠️ Criar script de start remoto
        run: |
          cat << 'EOF' > remote-restart.sh
          #!/bin/bash

          echo "🚨 Verificando processo com o JAR ${{ secrets.JAR_NAME }}"
          PID=$(pgrep -f ${{ secrets.JAR_NAME }})
          if [ -n "$PID" ]; then
            echo "🔪 Matando processo $PID..."
            kill -9 $PID
            echo "✅ Processo finalizado."
          else
            echo "ℹ️ Nenhum processo rodando."
          fi

          echo "⌛ Aguardando..."
          sleep 3

          echo "📂 Acessando pasta ${{ secrets.REMOTE_APP_PATH }}"
          cd ${{ secrets.REMOTE_APP_PATH }}

          echo "📍 Diretório atual: $(pwd)"
          echo "📄 Conteúdo:"
          ls -l

          echo "🚀 Iniciando app com nohup..."
          nohup java -jar ${{ secrets.JAR_NAME }} > nohup.out 2>&1 &

          echo "✅ App iniciado!"
          EOF

      - name: 📤 Copiar arquivos (JAR + Script) via SCP
        run: |
          scp -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no \
            target/${{ secrets.JAR_NAME }} \
            remote-restart.sh \
            ${{ secrets.LIGHTSAIL_USER }}@${{ secrets.LIGHTSAIL_HOST }}:${{ secrets.REMOTE_APP_PATH }}

      - name: 🚀 Executar script de restart remoto via SSH
        run: |
          ssh -i ~/.ssh/id_rsa \
              -o StrictHostKeyChecking=no \
              ${{ secrets.LIGHTSAIL_USER }}@${{ secrets.LIGHTSAIL_HOST }} \
              "chmod +x ${{ secrets.REMOTE_APP_PATH }}/remote-restart.sh && ${{ secrets.REMOTE_APP_PATH }}/remote-restart.sh"
