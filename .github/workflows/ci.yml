name: CI Pipeline

permissions:
  contents: read
  pull-requests: write
  security-events: write

on:
  push:
    branches-ignore:
      - main
      - develop
      - homolog

jobs:
  build-and-test:
    name: 🛠️ Build and Test
    runs-on: ubuntu-latest

    steps:
      - name: ⏬ Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: ⚙️ Build with Maven
        run: mvn clean compile

      - name: 🧪 Run Tests
        run: mvn test

  codeql-analysis:
    name: 🔍 CodeQL Analysis
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: ⏬ Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: 🚀 Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      - name: ⚙️ Build with Maven (CodeQL)
        run: mvn clean compile -DskipTests

      - name: 🔍 Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  create-pr-to-develop:
    name: 🔁 Create PR to develop
    runs-on: ubuntu-latest
    needs: codeql-analysis
    if: |
      startsWith(github.ref, 'refs/heads/') &&
      github.ref != 'refs/heads/develop' &&
      github.ref != 'refs/heads/main' &&
      github.ref != 'refs/heads/homolog'

    steps:
      - name: 🔁 Criar Pull Request para develop
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const branch = '${{ github.ref_name }}';
            const prTitle = `PR automático | ${branch} -> develop`;
            const prBody = `Este PR foi gerado automaticamente após o push na branch \`${branch}\`.`;

            const { data: existingPRs } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: `${context.repo.owner}:${branch}`,
              base: 'develop',
              state: 'open'
            });

            if (existingPRs.length === 0) {
              await github.rest.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: prTitle,
                head: branch,
                base: 'develop',
                body: prBody
              });
              console.log(`✅ PR criado: ${branch} -> develop`);
            } else {
              console.log('⚠️ PR já existente. Nenhuma ação tomada.');
            }

  create-pr-to-main:
    name: 🔁 Create PR to Main
    runs-on: ubuntu-latest
    needs: codeql-analysis
    if: github.ref == 'refs/heads/develop'

    steps:
      - name: 🔁 Criar Pull Request para main
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prTitle = `PR automático | develop -> main`;
            const prBody = `Este PR foi gerado automaticamente após a análise da branch \`develop\`.`;

            const { data: existingPRs } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: `${context.repo.owner}:develop`,
              base: 'main',
              state: 'open'
            });

            if (existingPRs.length === 0) {
              await github.rest.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: prTitle,
                head: 'develop',
                base: 'main',
                body: prBody
              });
              console.log('✅ PR criado: develop -> main');
            } else {
              console.log('⚠️ PR já existente. Nenhuma ação tomada.');
            }
